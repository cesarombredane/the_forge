{
  "name": "the_forge",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@9.12.0",
  "pnpm": {
    "overrides": {
      "esbuild": "^0.25.0",
      "postcss": "^8.5.10"
    }
  },
  "scripts": {
    "dev:web": "pnpm --filter @the-forge/web dev",
    "dev:api": "pnpm --filter @the-forge/api dev",
    "dev": "pnpm -r --parallel --stream dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  }
}